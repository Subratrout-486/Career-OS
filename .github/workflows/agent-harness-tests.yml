name: Career OS — Agent Harness Tests

on:
  pull_request:
    paths:
      - 'src/career_os/agent_harness.py'
      - 'src/career_os/agent_runtime.py'
      - 'tests/test_agent_harness.py'
      - 'docs/DEEPSEEK-HARNESS-ADAPTATION.md'
  push:
    branches: [main]
    paths:
      - 'src/career_os/agent_harness.py'
      - 'src/career_os/agent_runtime.py'
      - 'tests/test_agent_harness.py'
      - 'docs/DEEPSEEK-HARNESS-ADAPTATION.md'

jobs:
  harness:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - name: Install Career OS and test tools
        run: |
          python -m pip install --upgrade pip
          pip install -e .
          pip install pytest pytest-asyncio
      - name: Compile harness runtime
        run: python -m compileall -q src/career_os/agent_harness.py src/career_os/agent_runtime.py tests/test_agent_harness.py
      - name: Run harness contract tests
        run: python -m pytest tests/test_agent_harness.py -q --tb=short
