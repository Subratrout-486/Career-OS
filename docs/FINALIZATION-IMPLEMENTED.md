# Finalization implementation note

The finalization work is being delivered in small validated changes rather than a large unverified rewrite. This marker records the release contract before implementation changes are merged to `main`.

The working branch is `finalize/stability-job-pipeline`.
